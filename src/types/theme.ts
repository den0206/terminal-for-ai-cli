/**
 * Shared theme type definitions
 * Re-exports from shared types for backward compatibility
 */

export type {
  ThemePalette,
  ThemePreview,
  ThemePreset,
  ThemePresetKey,
  ThemePresetInfo,
} from '../shared/types';
